<div id="callback_form_container">
  <h4 class="text-xl font-bold text-yellow-400 mb-3">Замовити дзвінок</h4>
  <p class="text-sm text-gray-400 mb-4">Введіть ваш номер телефону, і ми зателефонуємо вам за 5 хвилин.</p>

  <%= form_with model: request, local: true do |form| %>
    
    <%# Відображення помилок валідації (працює після render :new) %>
    <% if request.errors.any? %>
      <div class="bg-red-900/50 text-red-100 p-2 rounded-lg text-sm mb-3">
        <% request.errors.full_messages.each do |msg| %>
          <p><%= msg %></p>
        <% end %>
      </div>
    <% end %>

    <div class="mb-3">
      <%# 🌟 ДОДАНО: Атрибут maxlength: 13 для ограничения длины 🌟 %>
      <%= form.text_field :phone_number, 
                          placeholder: "+380...", 
                          class: "w-full bg-gray-700 text-gray-100 p-3 rounded-lg border border-gray-600 focus:border-yellow-400 focus:ring", 
                          oninput: "formatPhoneNumber(this)",
                          maxlength: 13 %>
    </div>

    <%= form.submit "📞 Зателефонуйте мені", 
                    class: "w-full bg-yellow-600 text-gray-900 p-3 rounded-lg font-bold hover:bg-yellow-500 transition cursor-pointer" %>
  <% end %>
</div>